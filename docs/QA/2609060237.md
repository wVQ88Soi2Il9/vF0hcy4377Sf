# 2609060237

- **status:** resolved
- **topic:** Core refactor: remove reversible_operation_factory and adapt reversible_operation

## Questions and Answers

### Q1 · (human: wVQ88Soi2Il9)
Refactor Core `src/core/definition_iii.ts`, remove `reversible_operation_factory`, leaving only:
```ts
export interface reversible_operation extends namespaced_id 
{
    execute(sp: space,...args: any[]): void;
    inverse(sp: space,...args: any[]): void;
    other_info?: Record<string, any>;
}
```

### A1 · (agent: gemini-3.8-flash-high)
After removing `reversible_operation_factory`, two directions were evaluated for `pack_module.operations`:
- Option A (Recommended): Change to `Record<string, reversible_operation>`, maintaining consistency with `commands?: Record<string, cmd>`.
- Option B: Remove `operations` from `pack_module` entirely.
Decision: Adopted Option A, typing `pack_module.operations` as `Record<string, reversible_operation>`.

### Q2 · (agent: gemini-3.8-flash-high)
With `...args: any[]` added to `reversible_operation`'s `execute` and `inverse`, does `src/core/history.ts` replay (Undo/Redo) need adjustment?

### A2 · (human: wVQ88Soi2Il9)
Maintain `history.ts` as-is (passing only `sp`). `...args` is an optional extension left to concrete implementations or internal closures.

### Q3 · (agent: gemini-3.8-flash-high)
After removing `core.reversible_operation_factory`, how should the downstream `get_operation` utility in `src/packs/vanilla_alpha/query.ts` be handled?

### A3 · (human: wVQ88Soi2Il9)
Change `get_operation`'s return type to `core.reversible_operation`, preserving symmetry with `pack_module` fields.

### Q4 · (agent: gemini-3.8-flash-high)
Does this refactoring require dedicated unit tests for `reversible_operation`'s `...args` behavior?

### A4 · (human: wVQ88Soi2Il9)
Add unit tests in `tests/` verifying execution and inverse operations with custom `...args`.

## Conclusion
1. Removed `reversible_operation_factory` in `src/core/definition_iii.ts`.
2. Updated `reversible_operation` to accept `...args: any[]`.
3. Updated `pack_module.operations` to `Record<string, reversible_operation>`.
4. Kept `src/core/history.ts` calling `op.execute(sp)` and `op.inverse(sp)` without extra arguments.
5. Updated `src/packs/vanilla_alpha/query.ts` `get_operation` to return `core.reversible_operation`.
6. Added unit test `tests/reversible_operation.test.ts` covering `...args` behavior.

#core #reversible-operation #history #pack-module #grill-me
