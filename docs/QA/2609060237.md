# 2609060237

- **status:** resolved
- **topic:** 重構 Core: 取消 reversible_operation_factory 並調整 reversible_operation

## 提問與回答

### Q1 ·（human: wVQ88Soi2Il9）
重構 core `src/core/definition_iii.ts`，取消 `reversible_operation_factory`，只留下：
```ts
export interface reversible_operation extends namespaced_id 
{
    execute(sp: space,...args: any[]): void;
    inverse(sp: space,...args: any[]): void;
    other_info?: Record<string, unknown>;
}
```

### A1 ·（agent: gemini-3.8-flash-high）
在取消 `reversible_operation_factory` 後，針對 `pack_module.operations` 欄位有兩個處置方向：
- 方案 A（推薦）：改為 `Record<string, reversible_operation>`，與 `commands?: Record<string, cmd>` 保持一致的物件介面。
- 方案 B：直接從 `pack_module` 移除 `operations` 欄位，因為目前所有 Pack 皆未曾於模組靜態註冊 operations。
經決斷，採納方案 A：將 `pack_module.operations` 型別調整為 `Record<string, reversible_operation>`。

### Q2 ·（agent: gemini-3.8-flash-high）
在 `reversible_operation` 的 `execute` 與 `inverse` 增加 `...args: any[]` 後，`src/core/history.ts` 的執行與重放機制（Undo/Redo）是否需要跟著變更？

### A2 ·（human: wVQ88Soi2Il9）
維持 `history.ts` 現狀（僅傳入 `sp`），`...args` 為選填擴充，由實作者自行決定是否需要或在其內部封裝。

### Q3 ·（agent: gemini-3.8-flash-high）
在移除 `core.reversible_operation_factory` 後，下游引用到該型別的 `src/packs/vanilla_alpha/query.ts` 內的 `get_operation` 工具函式應如何處置？

### A3 ·（human: wVQ88Soi2Il9）
同步將 `get_operation` 的回傳型別改為 `core.reversible_operation`，保持 query 工具函式對 `pack_module` 欄位的對稱性。

### Q4 ·（agent: gemini-3.8-flash-high）
本次重構是否需要為 `reversible_operation` 的 `...args` 擴充行為補充專屬單元測試？

### A4 ·（human: wVQ88Soi2Il9）
在 `tests/` 補充單元測試，驗證 `reversible_operation` 接受自訂 `...args` 的執行與撤銷行為。

## 結論
1. `src/core/definition_iii.ts` 移除 `reversible_operation_factory`。
2. `reversible_operation` 介面更新為接受 `...args: any[]`。
3. `pack_module.operations` 型別改為 `Record<string, reversible_operation>`。
4. `src/core/history.ts` 維持原狀，呼叫 `op.execute(sp)` 與 `op.inverse(sp)` 不傳遞額外參數。
5. `src/packs/vanilla_alpha/query.ts` 的 `get_operation` 同步回傳 `core.reversible_operation`。
6. 新增單元測試 `tests/reversible_operation.test.ts` 覆蓋 `...args` 的執行與可逆性。

#core #reversible-operation #history #pack-module #grill-me
