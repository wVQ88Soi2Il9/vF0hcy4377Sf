# 2609060253

- **status:** resolved
- **topic:** vanilla_alpha 可逆操作改進：Fail-Fast 錯誤校驗與 other_info 命名空間標準化

## 提問與回答

### Q1 ·（human: wVQ88Soi2Il9）
這個有沒有能改進的部分 `src/packs/vanilla_alpha/operations.ts`？

### A1 ·（agent: gemini-3.8-flash-high）
提出了 5 項改進評估與雙向論證：
1. 物件架構：改用 Class 實作 vs. 維持 Closure 物件字面量
2. 介面對稱性：補齊移動與配方操作專屬介面 vs. 維持最小介面
3. 錯誤處理：加入 Fail-Fast 校驗 vs. 維持靜默忽略（Silent Fallback）
4. 重複邏輯：抽離 UID 容量修復輔助函式 vs. 維持行內就地處理
5. 命名空間：標準化 other_info 的 Pack Key（改為 vanilla_alpha）vs. 維持現有 'core' Key

### Q2 ·（human: wVQ88Soi2Il9）
處理 5, 3。

### A2 ·（agent: gemini-3.8-flash-high）
確認執行項目 5 與項目 3：
- 項目 5：將 `operations.ts` 內 4 個操作的 `other_info.core` 統整為 `other_info.vanilla_alpha`，落實 Pack 命名空間隔離規範。
- 項目 3：在 `delete_device_operation`、`move_device_operation` 與 `select_recipe_operation` 的 `execute` 及 `inverse` 中，加入目標裝置是否存在之 Fail-Fast 校驗，找不到裝置時拋出明確的 `Error`。

## 結論
1. `create_device_operation`、`delete_device_operation`、`move_device_operation`、`select_recipe_operation` 的 metadata 欄位統一使用 `other_info.vanilla_alpha`。
2. 在 `delete_device_operation`、`move_device_operation`、`select_recipe_operation` 執行與反向撤銷時，若目標 UID 不存在於 `sp.devices` 則拋出 Fail-Fast 例外。

#vanilla-alpha #operations #fail-fast #namespace #other-info
