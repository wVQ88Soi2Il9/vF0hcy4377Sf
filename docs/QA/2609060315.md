# 2609060315

- **status:** resolved
- **topic:** vanilla_alpha initialization convergence: remove init.ts and consolidate into index.ts

## Questions and Answers

### Q1 · (human: wVQ88Soi2Il9)
Why do both `src\packs\vanilla_alpha\init.ts` and `src\packs\vanilla_alpha\index.ts` have Init?

### A1 · (agent: gemini-3.8-flash-high)
Prior commit `16a6380` added simplified `global_init`/`world_init` at the bottom of `index.ts`, causing ES Module export shadowing over the full Hook definitions in `init.ts`.
Two options were presented:
- Option A: Clean up `index.ts` to simply re-export `init.ts`.
- Option B: Delete `init.ts`, consolidating all initialization logic into `index.ts`.

### Q2 · (human: wVQ88Soi2Il9)
Option B: Delete init.ts, consolidate initialization logic into index.ts.

### A2 · (agent: gemini-3.8-flash-high)
Adopted Option B:
1. Merged the 10 core Hooks and `world_init` event-forwarding logic from `init.ts` directly into `src/packs/vanilla_alpha/index.ts`.
2. Removed `export * from './init';` in `index.ts`.
3. Deleted `src/packs/vanilla_alpha/init.ts`.

## Conclusion
1. `src/packs/vanilla_alpha/index.ts` serves as the sole initialization and export entrypoint for the Pack, housing complete Hook declarations and event forwarding.
2. Deleted the redundant `src/packs/vanilla_alpha/init.ts`.

#vanilla-alpha #init #index #lifecycle #hooks
