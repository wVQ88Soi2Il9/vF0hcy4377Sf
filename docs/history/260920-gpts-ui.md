# gpts_ui 功能導向重寫

狀態：等待 Human 驗收。

先由 shirones_ui 整理 `src/packs/gpts_ui/FEATURES.md`，再以功能清單和上游契約撰寫獨立 TS/CSS。新版保留版面、設備 CRUD／recipe／擴充、CLI、歷史分支視覺化與導航能力。Viewport 仍僅有容器與縮放顯示。

新 UI 由呼叫端明確指定 world，擴充由可共用的 registry 物件管理。沒有 singleton bound_world，也沒有修改 core、world 或其他 packs。新增 `gpts-ui.html` 預覽；Human 後續指示已將 `src/main.ts` 主入口切換至 GPTs UI。

校驗通過：專案 TypeScript／build、新入口獨立 Vite build、校驗程式 TypeScript，以及 `checks/gpts_ui` 的 39 項瀏覽器行為與 7 項真實 pointer 拖曳／尺寸保留檢查。初次 Vite 執行受 sandbox spawn EPERM 限制，經 sandbox 外執行成功。展開版面截圖已檢視；沒有要求與舊版像素相同。
