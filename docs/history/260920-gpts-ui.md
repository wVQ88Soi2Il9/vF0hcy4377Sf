# gpts_ui 功能導向重寫

狀態：等待 Human 驗收。

先由 shirones_ui 整理 `src/packs/gpts_ui/FEATURES.md`，再以功能清單和上游契約撰寫獨立 TS/CSS。新版保留版面、設備 CRUD／recipe／擴充、CLI、歷史分支視覺化與導航能力。Viewport 仍僅有容器與縮放顯示。

新 UI 由呼叫端明確指定 world，擴充由可共用的 registry 物件管理。沒有 singleton bound_world。Human 後續指示已將 `src/main.ts` 主入口切換至 GPTs UI，獨立 `gpts-ui.html` 與 `preview.ts` 因功能重複而移除。

校驗通過：專案 TypeScript／build、校驗程式 TypeScript，以及 `checks/gpts_ui` 的瀏覽器行為與真實 pointer 拖曳／尺寸保留檢查。初次 Vite 執行受 sandbox spawn EPERM 限制，經 sandbox 外執行成功。展開版面截圖已檢視；沒有要求與舊版像素相同。

後續接入 `master/src/packs/test`。因來源使用已移除的 layered_2d、vanilla、global world 與舊 renderer/recipe 契約，設備改寫為目前 core.device，保留六個 ID、shape、ports、顏色與 pipe path；兩個 recipe 改用目前的 output contract。main 只註冊 test pack，並將 world 與畫面區域交給 GPTs UI；test pack 不依賴特定 UI。
