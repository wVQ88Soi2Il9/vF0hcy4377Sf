# 開發 TODO List 202608122204：basic_renderer 與 basic_ui N 維空間 (N-Dim) 全面擴充

## 1. 背景與目標

引擎核心 (`core/types.ts`) 的座標 `vector` 為 $N$ 維陣列（`number[]`），地圖維度由 `map.size.length` 動態決定。  
目前 `basic_renderer` 與 `basic_ui` 主要針對 3D 空間處理，現需將繪製與 UI 控制全面重構與擴充，以完整支援任意 $N$ 維空間（N-Dimensional Space）。

---

## 2. 待辦事項 (TODO List)

### Phase 1：basic_renderer N 維空間擴充
- [ ] **重構 `src/packs/basic_renderer/`**
  - [ ] **切片與維度對應**：`camera.plane.slices` 動態依據 `map.size.length` 初始化與適應。
  - [ ] **N 維 Cell 可視性過濾**：`draw_device.ts` 中確保僅繪製所有隱藏維度恰好符合 `slices[i]` 切片深度的 N 維 Cell。
  - [ ] **N 維地圖邊界繪製**：`draw_grid.ts` 依據 `camera.plane.dim_h` 與 `dim_v` 動態取得世界長度畫出對應 2D 邊界。
  - [ ] **任意雙軸投影**：允許 `dim_h` 與 `dim_v` 為任意 $0 \le i < N$ 且 $i \neq j$ 的維度索引。

### Phase 2：basic_ui Info Bar 放置為空 (Deferred)
- [x] **重構 `src/packs/basic_ui/info_bar.ts`**
  - [x] 暫放置 Info Bar 内容，改為純空組件 (`display: none; width: 0`)，避免遮擋與不必要維護。

### Phase 3：basic_ui CMD 指令 N 維化
- [ ] **重構 `src/packs/basic_ui/cmd_executor.ts`**
  - [ ] **N 維 `create`**：支援輸入 $N$ 個座標值（例如 `create test:assembler 4 4 0 1`）。
  - [ ] **N 維 `move`**：支援輸入 $N$ 個座標值（例如 `move 0 6 6 0 2`）。
  - [ ] **N 維 `camera` 方程式**：支援同時固定 $N-2$ 個維度切片（例如 `camera --"z=0, w=1, d4=2"`）。

### Phase 4：驗證與測試
- [ ] 測試建立 4D 地圖（如 `size = [20, 20, 5, 5]`），驗證 `camera` 與 `create` 指令能正常切面與放置設備。
- [ ] 執行 `npx tsc -b` 驗證 TypeScript 型別檢查無誤。
