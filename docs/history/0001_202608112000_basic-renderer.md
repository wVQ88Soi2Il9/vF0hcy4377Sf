# 0001_202608112000_basic-renderer

- **status:** done
- **prev:** none
- **skill:** plan-history v3

## 主題簡述

完成 `src/packs/basic_renderer` 的實作，並在過程中確定多項架構決策（Core import 放寬、API.ts 定位、runtime.ts 誕生與獨立 draw_registry），更新規範文件。

## 觀察與推論

### O1 · 2026-08-11 20:00:00+08:00 — basic_renderer 與架構規範完成
Core import 改為僅禁止直接操作 hooks singleton；API.ts 為純事件契約；runtime.ts 負責啟動期狀態。

## 待辦

### 1 basic_renderer 實作與架構決策確定
- **state:** 完成
- **basis:** → O1

完成 `src/packs/basic_renderer` 實作，確立 Core import 放寬規則、API.ts 為純事件契約、獨立 `src/runtime.ts` 容器，以及將 `register_device_draw` 住在 `draw_registry`。

**沿革**
- H1 · 2026-08-11 20:00 落地 —— 完成 basic_renderer 與架構決策 → O1
